import 'package:flutter/material.dart';

class AppAnimations {
  static const headline = Duration(milliseconds: 400);
  static const description = Duration(milliseconds: 350);
  static const pageTransition = Duration(milliseconds: 320);
  static const indicator = Duration(milliseconds: 250);
  static const footer = Duration(milliseconds: 280);

  static const material = Curves.easeOutCubic;
}
